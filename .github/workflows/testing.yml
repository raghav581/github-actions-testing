name: Print Name
on:
  push:
jobs:
  print:
    runs-on: ubuntu-16.0
    steps:
      - name: Setup
        uses: actions/checkout@v4
      - name: Print name
        run: echo "Hello There, I am Raghav"
      - name: Print name
        run: node index.test.js