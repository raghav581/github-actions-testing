name: Print Name
on:
  push:
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Setup
        uses: actions/checkout@v4
      - name: Print name
        run: echo "Hello There, I am Raghav"
      - name: Print name
        run: node index.test.js